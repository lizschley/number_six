from common_classes.paragraphs_for_display import ParagraphsForDisplay


def context_to_paragraphs(context):
    paragraphs = ParagraphsForDisplay()
    print(f'in paragraph finders context to paragraphs, context=={context}')
    # paragraphs.retrieve_paragraphs(context)
    return paragraphs
